[
   {don_erleone, 
    [
      {ollama_model, "qwen3.5:9b"},
      {ollama_url, "http://ollama-internal.ollama.svc.cluster.local:11434/api/generate"},
      {mcp_url, "http://kubernetes-mcp.kubernetes-mcp.svc.cluster.local:30090/mcp"},
      {timeout, 3600000},
      {sub_model, "qwen3.5:9b"},
      {sub_timeout, 120000}
    ]
   },
   {mnesia, [{dir, "/data/mnesia"}]},
   {kernel, [
       {logger_level, info},
       {logger, [
           {handler, default, logger_std_h, #{
               formatter => {logger_formatter, #{
                   single_line => true,
                   template => [time, " [", level, "] ", msg, "\n"]
               }}
           }}
       ]}
   ]},
   {sasl, [{sasl_error_logger, false}]}
].